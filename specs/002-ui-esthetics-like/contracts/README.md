# Contracts: UI Aesthetics

Leverage existing legend endpoint. Add preference endpoints if needed in future:
- GET /preferences/ui — get current user UI preferences
- PUT /preferences/ui — update theme and font size preset

Schemas to be defined when implementing persistence.
